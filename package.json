{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
"build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "db:push": "node --env-file-if-exists=.env scripts/run.mjs --filter @workspace/db run push",
    "db:seed": "node --env-file-if-exists=.env scripts/run.mjs --filter @workspace/scripts run seed"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
